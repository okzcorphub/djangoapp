apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: blogzine-default
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/blogzine-default: "true"
  podSelector:
    matchLabels:
      io.kompose.network/blogzine-default: "true"
